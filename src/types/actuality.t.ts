export interface ActualityCreation {
    id: Number,
    title: string,
    date: Date,
    description: string
}

export type ActualityFinal = {
    id: Number,
    title: string,
    date: Date,
    description: string
}